<div class="navbar navbar-expand-lg navbar-light navbar-lewagon border-bottom  d-flex justify-content-between">
    <div>
      <%= link_to root_path do  %>
            <%= image_tag 'logo.png', class: "logo ml-2" %>   
      <% end %> 
    </div>
        <div>
         <%= link_to "Back", bookings_path, class: "btn border mr-1"%>
         <%= link_to "SignOut", destroy_user_session_path, method: :delete, class: "btn rounded text-white signout-button mr-4"%>
    </div>
</div>

<div class="container full-page p-3">
  <div class="row d-flex justify-content-center">
    <h2 style="color: rgba(0, 0, 0, 0.5);">Edit your booking</h2>
  </div>
  <div class="row mt-2">
    <div class="col-6">
      <h4 style="color: rgba(0, 0, 0, 0.5);"><%= @art.name %></h4>
      <p style="color: rgba(0, 0, 0, 0.5);">£<%= @art.rate %> per day</p>
      <%= cl_image_tag @art.photo.key, height: 300, width: 400, crop: :fill %>
    </div>
    <div class="col-6">
      <%= render 'update_booking_form', class: "sign-up" %>
      <div class="mt-2">
        <%= link_to 'Cancel Booking', booking_path(@booking), method: :delete, data: {confirm: "Are you sure you want to cancel this booking?"}, class: "btn btn-outline-dark"  %>    
      </div>
    </div>
  </div>
</div>