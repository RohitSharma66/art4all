<div class="navbar navbar-expand-lg navbar-light navbar-lewagon border-bottom  d-flex justify-content-between">
    <!-- <div class="text-secondary"> -->
		<div>
			<%= link_to root_path do  %>
		        <%= image_tag 'logo.png', class: "logo ml-1" %>		
			<% end %>	
		</div>
        <div>
			<%= link_to "Back", arts_path, class: "btn mr-3", style:"background-color:#5bc0be" %>				
		</div>
    <!-- </div> -->
</div>

<div class="container full-page">
  <h3 class="text-center p-4">View Desk</h3>
	<div class="row mb-4 mt-5">
		<!-- < % @arts.each do |art| %> -->
			<%= link_to art_path(@art) do %>
				<div class="col-12 col-md-4">
					<% if @art.photo.attached? %>
					<%= cl_image_tag @art.photo.key, height: 300, width: 250, crop: :fill %>
					<% end %>		
				<% end %>
				</div>
					<div class="card-content">		
					<%= @art.name %>
					<%= @art.artist %>
					<%= @art.price %>	
					</div>
			<!-- < % end %> -->
	</div>
	<%= link_to 'Delete', art_path(@art), method: :delete, class:"btn btn-danger" %>
</div>